{
  "framework": "nextjs",
  "buildCommand": "node scripts/ensure-direct-url.mjs && node scripts/prisma-migrate-deploy.mjs && prisma generate && next build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/quarterly-sms-reminders",
      "schedule": "0 9 * * *"
    }
  ]
}
